[alias]
        aliases = !git config --get-regexp 'alias.*' | colrm 1 6 | sed 's/[ ]/ = /'
        author = "!f() { git log --author="$1" ; }; f"
        self-blame = "!f() { git log --author=\"$(git config user.name)\" -p --follow $1 ; }; f"
        ci = commit
        co = checkout
        pull = pull --rebase
        unmerged = diff --name-only --diff-filter=U
        parent = remote show origin
        root = rev-parse --show-toplevel
        st = status -uno
        df = diff -w
        bl = blame -wM
[color]
        ui = auto
[core]
        preloadindex = true
        sharedrepository = 0640
[diff]
        mnemonicprefix = true
        renames = true
[http]
        proxy = https://proxy-chain.intel.com:912
[intel]
        clone-minimum-free-kb = 10485760
        userInitVersion = 2
[log]
        decorate = true
[merge]
        tool = p4merge
        conflictstyle = diff3
        renamelimit = 999999
[mergetool "kdiff3_noauto"]
        cmd = /p/hdk/rtl/proj_tools/git/da_tools/14.06.0/kdiff3-noauto.sh $TOOL_MODE $base_present $MERGED $LOCAL $REMOTE $BASE
[mergetool "p4merge"]
        path = /usr/intel/bin/p4merge
[pack]
        threads = 0
[push]
        default = tracking
[receive]
        denycurrentbranch = refuse
[user]
	useconfigonly = true
[pull]
	rebase = true
